package sample.message.request;

import javafx.stage.Stage;
import sample.message.AbstractStageMessage;

public class RequestEditorStage extends AbstractStageMessage {


    public RequestEditorStage(Stage stage) {
        super(stage);
    }
}
