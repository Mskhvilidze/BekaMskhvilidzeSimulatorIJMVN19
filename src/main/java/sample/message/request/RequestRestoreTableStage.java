package sample.message.request;

import javafx.stage.Stage;
import sample.message.AbstractStageMessage;

public class RequestRestoreTableStage extends AbstractStageMessage {
    public RequestRestoreTableStage(Stage stage) {
        super(stage);
    }
}
