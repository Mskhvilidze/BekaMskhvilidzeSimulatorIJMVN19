package sample.message.request;

import javafx.stage.Stage;
import sample.message.AbstractStageMessage;


public class RequestExitStage extends AbstractStageMessage {

    public RequestExitStage(Stage stage) {
        super(stage);
    }
}
