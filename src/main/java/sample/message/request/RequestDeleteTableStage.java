package sample.message.request;

import javafx.stage.Stage;
import sample.message.AbstractStageMessage;

public class RequestDeleteTableStage extends AbstractStageMessage {
    public RequestDeleteTableStage(Stage stage) {
        super(stage);
    }
}
